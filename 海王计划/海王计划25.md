# 海王计划25

[TOC]



## [123. 买卖股票的最佳时机 III](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iii/)

**关键词**：：多维动态规划

~~~c++
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int k = 3;
        vector<int> buys(k, INT_MIN), sells(k, 0);

        for(int i : prices){
            for(int j = 1; j<k; j++){
                buys[j] = max(buys[j], sells[j-1] - i);
                sells[j] = max(sells[j], buys[j] + i);
            }
        }

        return sells[k-1];
    }
};
~~~



## [188. 买卖股票的最佳时机 IV](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-iv/)

**关键词**：多维动态规划

~~~c++
class Solution {
public:
    int maxProfit(int k, vector<int>& prices) {
        k++;
        vector<int> buys(k, INT_MIN), sells(k, 0);

        for(int i : prices){
            for(int j = 1; j<k; j++){
                buys[j] = max(buys[j], sells[j-1] - i);
                sells[j] = max(sells[j], buys[j] + i);
            }
        }

        return sells[k-1];
    }
};
~~~



## [72. 编辑距离](https://leetcode.cn/problems/edit-distance/)

**关键词**：多维动态规划

~~~c++
class Solution {
public:
    int minDistance(string word1, string word2) {
        int n = word1.size(), m = word2.size();

        //有一个字符串为空
        if(n*m == 0) return n+m;

        vector<vector<int>> cnts(n+1, vector<int>(m+1));

        for(int i = 0; i<n+1; i++){
            cnts[i][0] = i;
        }
        for(int j = 0; j<m+1; j++){
            cnts[0][j] = j;
        }

        for(int i = 1; i<n+1; i++){
            for(int j = 1; j<m+1; j++){
                if(word1[i-1] == word2[j-1]){
                    cnts[i][j] = min(cnts[i-1][j] + 1, min(cnts[i][j-1] + 1, cnts[i-1][j-1]));
                }
                else{
                    cnts[i][j] = min(cnts[i-1][j] + 1, min(cnts[i][j-1] + 1, cnts[i-1][j-1] + 1));
                }
            }
        }

        return cnts[n][m];
    }
};
~~~



## [133. 克隆图](https://leetcode.cn/problems/clone-graph/)

**关键词**：图，DFS

~~~c++
/*
// Definition for a Node.
class Node {
public:
    int val;
    vector<Node*> neighbors;
    Node() {
        val = 0;
        neighbors = vector<Node*>();
    }
    Node(int _val) {
        val = _val;
        neighbors = vector<Node*>();
    }
    Node(int _val, vector<Node*> _neighbors) {
        val = _val;
        neighbors = _neighbors;
    }
};
*/

class Solution {
public:
    unordered_map<Node*, Node*> umap;

    Node* cloneGraph(Node* node) {
        if(node == nullptr){
            return node;
        }

        if(umap.find(node) != umap.end()){
            return umap[node];
        }

        Node* cloneNode = new Node(node->val);
        umap[node] = cloneNode;

        for(Node* i : node->neighbors){
            cloneNode->neighbors.emplace_back(cloneGraph(i));
        }

        return cloneNode;
    }
};
~~~



## [146. LRU 缓存](https://leetcode.cn/problems/lru-cache/)

**关键词**：双向链表，哈希表

~~~c++
struct DLinkedNode{
    int key, val;
    DLinkedNode* prev, * next;
    DLinkedNode():key(0), val(0), prev(nullptr), next(nullptr){}
    DLinkedNode(int i_key, int i_val):key(i_key), val(i_val), prev(nullptr), next(nullptr){}
};

class LRUCache {
public:
    LRUCache(int capacity) {
        cap = capacity;
        size = 0;
        head = new DLinkedNode();
        tail = new DLinkedNode();
        head->next = tail;
        tail->prev = head;
    }
    
    int get(int key) {
        if(umap.count(key)){
            moveToHead(umap[key]);
            return umap[key]->val;
        }
        return -1;
    }
    
    void put(int key, int value) {
        if(umap.count(key)){
            umap[key]->val = value;
            moveToHead(umap[key]);
        }
        else{
            DLinkedNode* node = new DLinkedNode(key, value);
            umap[key] = node;
            addToHead(node);
            if(size == cap){
                DLinkedNode* removeNode = removeTail();
                umap.erase(removeNode->key);
            }
            else{
                size++;
            }
        }
        
    }

    void addToHead(DLinkedNode* node){
        node->next = head->next;
        head->next->prev = node;
        head->next = node;
        node->prev = head;
    }

    void moveToHead(DLinkedNode * node){
        node->prev->next = node->next;
        node->next->prev = node->prev;
        node->next = head->next;
        node->prev = head;
        head->next->prev = node;
        head->next = node;
    }

    DLinkedNode* removeTail(){
        DLinkedNode* removeNode = tail->prev;
        removeNode->prev->next = tail;
        tail->prev = removeNode->prev;

        return removeNode;
    }

private:
    int cap, size;
    DLinkedNode* head, * tail;
    unordered_map<int, DLinkedNode*> umap;
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */
~~~

